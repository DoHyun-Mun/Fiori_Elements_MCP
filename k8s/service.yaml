apiVersion: v1
kind: Service
metadata:
  name: fiori-mcp-test
  namespace: fiori-mcp-test
  labels:
    app: fiori-mcp-test
spec:
  type: ClusterIP
  ports:
    - port: 4004
      targetPort: 4004
      protocol: TCP
  selector:
    app: fiori-mcp-test