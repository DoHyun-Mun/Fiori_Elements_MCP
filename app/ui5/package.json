{
  "name": "inventory-management-ui5",
  "version": "1.0.0",
  "description": "AI 점포 운영 — UI5 Shell + Dashboard + AI Chat + AI 결과 5앱",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "ui5 serve --open index.html",
    "build": "ui5 build --clean-dest --include-task=generateManifestBundle --include-task=generateCachebusterInfo"
  },
  "devDependencies": {
    "@ui5/cli": "^3.10.0",
    "@sap/ux-ui5-tooling": "^1.15.0"
  },
  "ui5": {
    "dependencies": [
      "@sap/ux-ui5-tooling"
    ]
  }
}